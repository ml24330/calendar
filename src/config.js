/* Things you might reasonably want to change, in one place.
   Edit, commit, deploy. No environment variables, no database row, no
   dashboard — the name is part of the app, so it lives with the code. */

export const ORG_NAME = "Stanford Economics Social Calendar";

// The academic year shown under the title. Bump this each September.
export const CUR_YEAR = 2026;

// The timezone the whole calendar runs on lives in src/lib/tz.js as
// CALENDAR_TZ, next to the conversion logic that uses it.
